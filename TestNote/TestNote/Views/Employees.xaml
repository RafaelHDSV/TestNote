<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:TestNote.ViewModels" 
             xmlns:models="clr-namespace:TestNote.Models"
             x:DataType="vm:EmployeesViewModel"
             x:Class="TestNote.Views.Employees"
             Title="Funcionários">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="+ Novo Funcionário" 
                     Command="{Binding GoToCreateEmployeeCommand}" 
                     IconImageSource="adicionar_funcionario.png" />
    </ContentPage.ToolbarItems>
    
    <Grid RowDefinitions="Auto, *">

        <ActivityIndicator Grid.Row="0" 
                           IsRunning="{Binding IsLoading}"
                           IsVisible="{Binding IsLoading}"
                           Color="#1976D2"
                           HorizontalOptions="Center" 
                           VerticalOptions="Start"
                           Margin="10"/>

        <VerticalStackLayout Grid.Row="1" Padding="20">

            <Label Text="{Binding Company.Name, StringFormat='Lista de Funcionários ({0})'}" 
                   FontSize="22" 
                   FontAttributes="Bold" 
                   Margin="0,0,0,15"
                   IsVisible="{Binding Company, Converter={StaticResource NullToBoolConverter}}"/>
            <CollectionView
                Grid.Row="1"
                ItemsSource="{Binding Employees}"
                SelectionMode="Single"
                SelectedItem="{Binding SelectedEmployee}">

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Employee">
                        <Border Margin="0,5" Padding="15"
                            StrokeShape="RoundRectangle 8"
                            BackgroundColor="White"
                            HorizontalOptions="Fill">

                            <Border.GestureRecognizers>
                                <TapGestureRecognizer 
                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:EmployeesViewModel}}, Path=SelectEmployeeCommand}"
                                CommandParameter="{Binding .}" />
                            </Border.GestureRecognizers>

                            <VerticalStackLayout Spacing="4">
                                <Label Text="{Binding Id}"
                                        TextColor="#000"
                                    />
                                <Label Text="{Binding Name}"
                                       TextColor="#000"
                                    />
                                <Label Text="{Binding JobTitle}"
                                        TextColor="#000"
                                    />
                                <Label Text="{Binding UserId}"
                                        TextColor="#000"
                                    />
                                <Label Text="{Binding CompanyId}"
                                        TextColor="#000"
                                    />
                                <Label Text="{Binding Email}"
                                        TextColor="#000"
                                    />
                                <Label Text="{Binding AccessLevel}"
                                        TextColor="#000"
                                    />
                                <Label Text="{Binding DisplayName}"
                                        TextColor="#000"
                                    />
                                <Label Text="{Binding RoleName}"
                                        TextColor="#000"
                                    />
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Label Text="Nenhum funcionário encontrado para esta empresa."
                   IsVisible="{Binding Employees.Count, Converter={StaticResource EmptyCollectionToBoolConverter}}"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"
                   Margin="0,50,0,0"
                   TextColor="#777"/>

        </VerticalStackLayout>
    </Grid>
</ContentPage>